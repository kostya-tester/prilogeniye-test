name: Build PKRV_Monitor EXE

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"  # Убирает ошибку, остаётся только предупреждение

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install paramiko pyinstaller

      - name: Build EXE
        run: pyinstaller --onefile --windowed --name PKRV_Monitor --hidden-import paramiko main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PKRV_Monitor
          path: dist/PKRV_Monitor.exe
